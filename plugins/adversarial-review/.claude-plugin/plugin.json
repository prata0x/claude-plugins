{
  "name": "adversarial-review",
  "description": "Deep, skeptical review of a caller-specified diff range using parallel scanner agents (correctness/regressions, design/maintainability, test coverage) plus a confidence filter — for moments needing more scrutiny than the global reviewer subagent alone, e.g. before a deploy.",
  "version": "0.1.0",
  "author": {
    "name": "prata0x"
  }
}
